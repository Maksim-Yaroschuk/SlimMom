import styled from 'styled-components';
import {
  color,
  layout,
  flexbox,
  typography,
  border,
  shadow,
  space,
  position,
  grid,
} from 'styled-system';

export const Box = styled.div`
  ${color};
  ${layout};
  ${flexbox};
  ${typography};
  ${border};
  ${shadow};
  ${space};
  ${position};
  ${grid};
`;
